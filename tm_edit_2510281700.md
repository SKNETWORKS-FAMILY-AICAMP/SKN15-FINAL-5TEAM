# 작업 일지 - 2025-10-28 17:00

## 📋 작업 개요

**작업 날짜:** 2025-10-28
**작업 시간:** 17:00
**브랜치:** remake
**작업자:** Claude Code

---

## 🐛 발견된 문제

### 1. 캐릭터 자동 대화 진행 문제 (Critical)

**증상:**
- 유저 입력 없이 캐릭터들(탄지로, 렌고쿠, 젠이츠, 이노스케, 엔무)이 알아서 대화를 진행
- 유저는 관람만 하게 되어 상호작용 불가능
- 백엔드 로그: `[INFO] [OPEN_NARRATIVE] ⚡ Auto-advancing after 4 turns (max: 3)`

**근본 원인:**
1. **open_narrative_stage.py**: `max_turns` 도달 시 자동으로 다음 스테이지로 진행
2. **children_agent.py**: `llm_beats=true` 설정 시 유저 입력 없이도 LLM이 beats(대화) 자동 생성

### 2. 'user' characterId 표시 오류

**증상:**
- 시나리오의 플레이어 캐릭터(렌고쿠의 제자) 대사가 실제 유저 메시지로 잘못 표시
- 보라색 말풍선, 오른쪽 정렬로 표시되어 혼란 발생

**원인:**
- `characterId === 'user'`를 실제 유저 입력으로 잘못 인식
- 'user'는 NPC 플레이어 캐릭터를 의미하는데 프론트엔드가 이를 구분하지 못함

---

## ✅ 해결 방법

### 수정 1: open_narrative_stage.py - 자동 진행 로직 제거

**파일:** `backend/src/agents/stage_handlers/open_narrative_stage.py`
**라인:** 60-69

**변경 전:**
```python
# 5. 턴 수 기반 자동 전환 체크
stage_complete = False
if turn_count >= max_turns or stage_turn >= max_turns:
    log("open_narrative", f"⚡ Auto-advancing after {turn_count} turns (max: {max_turns})")
    stage_complete = True

    # 전환 대사 생성
    transition_dialogue = self._create_transition_dialogue(
        state, context, speaker_pool, next_stage
    )

    return StageResult(
        children_ctx={...},
        stage_complete=True,
        next_stage=next_stage,
    )
```

**변경 후:**
```python
# 5. 턴 수 기반 자동 전환 체크 (비활성화)
# 문제: 유저 입력과 관계없이 max_turns 도달 시 자동 진행하여 캐릭터들이 알아서 대화 진행
# 해결: max_turns 체크를 제거하고, 유저가 직접 스테이지를 진행하도록 함
stage_complete = False

# max_turns 경고만 출력 (자동 진행 안 함)
if turn_count >= max_turns or stage_turn >= max_turns:
    log("open_narrative", f"⚠️  Turn limit reached: {turn_count}/{max_turns} (auto-advance disabled)")

# 자동 진행 로직 제거됨 - 유저가 명시적으로 다음 스테이지로 가는 입력을 해야 진행
```

**효과:**
- TRAIN_PRELUDE 스테이지에서 max_turns 도달해도 유저 입력 필요
- 유저가 스토리 진행을 직접 제어 가능

---

### 수정 2: children_agent.py - LLM Beats 생성 조건 추가

**파일:** `backend/src/agents/children_agent.py`
**라인:** 168-186

**변경 전:**
```python
if llm_beats_enabled:
    log("children", f"🎭 LLM Beats mode enabled for stage={stage_tag}")
    # llm_beats가 활성화되면 beats를 LLM이 즉흥 생성
    if not beats:
        # context를 기반으로 beats 생성 프롬프트를 만듦
        beats = self._generate_beats_from_context(state, ctx)
        log("children", f"✨ Generated {len(beats)} beats via LLM")
```

**변경 후:**
```python
if llm_beats_enabled:
    log("children", f"🎭 LLM Beats mode enabled for stage={stage_tag}")

    # 유저 입력이 있을 때만 beats를 LLM이 즉흥 생성
    # 문제: 유저 입력 없이 자동으로 beats를 생성하면 캐릭터들이 알아서 대화 진행
    # 해결: 유저 입력이 있을 때만 LLM beats 생성
    if not beats:
        latest_user_input = ctx.get("latest_user_input", "").strip()
        user_input = state.get("user_input", "").strip()

        # 유저 입력이 있을 때만 beats 생성
        if latest_user_input or user_input:
            # context를 기반으로 beats 생성 프롬프트를 만듦
            beats = self._generate_beats_from_context(state, ctx)
            log("children", f"✨ Generated {len(beats)} beats via LLM based on user input")
        else:
            # 유저 입력이 없으면 beats 생성하지 않음 (fallback으로 프롬프트 메시지 표시)
            log("children", "⚠️ LLM Beats mode: No user input detected, skipping auto-generation")
            # beats를 비워서 fallback 메시지 사용
```

**효과:**
- HEROES_ARRIVE 같은 `llm_beats=true` 스테이지에서도 유저 입력 필수
- 캐릭터들이 유저 입력에 반응하여 대화 진행

---

### 수정 3: ChatInterface.tsx - 'user' characterId NPC 처리

**파일:** `front/src/components/ChatInterface.tsx`

#### 3-1. 시나리오 메시지 처리 (라인 1127-1129)

**변경 전:**
```typescript
isUser: scene.characterId === 'user',  // 잘못된 로직
characterId: scene.characterId === 'user' ? undefined : scene.characterId,
```

**변경 후:**
```typescript
isUser: false,  // 시나리오 메시지는 모두 NPC
characterId: scene.characterId,  // 'user'도 NPC 캐릭터로 처리
```

#### 3-2. 프로필 이미지 매핑 추가 (라인 163)

```typescript
'user': '/images/프로필_탄지로.png',  // 플레이어 캐릭터 (렌고쿠의 제자)
```

#### 3-3. 캐릭터 이름 매핑 추가 (라인 183)

```typescript
'user': '츠구코',  // 플레이어 캐릭터 (렌고쿠의 제자 = 츠구코)
```

#### 3-4. 글로우 색상 매핑 추가 (라인 236-240)

```typescript
'user': {
  primary: '#F97316',  // 플레이어 캐릭터 - 오렌지 불꽃
  shadow: 'rgba(249, 115, 22, 0.6)',
  bg: 'linear-gradient(135deg, rgba(249, 115, 22, 0.08), rgba(249, 115, 22, 0.03))'
}
```

**효과:**
- 'user' characterId가 "츠구코"라는 NPC로 정확히 표시
- 왼쪽 정렬, 흰색 말풍선, 프로필 사진과 오렌지 글로우 효과

---

## 📊 수정된 파일 목록

### Backend
1. `backend/src/agents/stage_handlers/open_narrative_stage.py`
   - max_turns 자동 진행 로직 제거
   - 경고 메시지만 출력하도록 변경

2. `backend/src/agents/children_agent.py`
   - llm_beats 생성 시 유저 입력 필수 조건 추가
   - 유저 입력 없으면 beats 생성 스킵

### Frontend
3. `front/src/components/ChatInterface.tsx`
   - 'user' characterId를 NPC로 처리
   - 프로필, 이름, 색상 매핑 추가

---

## 🧪 테스트 결과

### Before (문제 상황)
1. ✗ TRAIN_PRELUDE에서 3번 입력 후 자동으로 HEROES_ARRIVE로 진행
2. ✗ HEROES_ARRIVE에서 유저 입력 없이 LLM이 beats 자동 생성
3. ✗ 탄지로, 렌고쿠, 젠이츠, 이노스케가 알아서 대화 진행
4. ✗ 플레이어 캐릭터 대사가 "user"로 표시

### After (해결)
1. ✓ TRAIN_PRELUDE에서 max_turns 도달해도 유저 입력 대기
2. ✓ HEROES_ARRIVE에서 유저가 입력해야만 LLM beats 생성
3. ✓ 캐릭터들이 유저 입력에 반응하여 대화 진행
4. ✓ 플레이어 캐릭터 대사가 "츠구코"로 정확히 표시

---

## 🔍 기술적 세부사항

### 자동 진행 방지 메커니즘

**open_narrative_stage.py:**
- `max_turns` 체크는 유지하되 자동 진행 로직 제거
- 로그만 출력하여 디버깅 정보 제공
- `stage_complete` 플래그를 수동으로만 설정 가능

**children_agent.py:**
- `latest_user_input` 또는 `user_input` 존재 여부 확인
- 두 필드 중 하나라도 있으면 beats 생성 허용
- 없으면 빈 beats 리스트로 fallback 메시지 표시

### NPC vs 유저 구분

**characterId 체계:**
- `'user'`: NPC 플레이어 캐릭터 (렌고쿠의 제자/츠구코)
- `isUser: true`: 실제 유저가 채팅창에 입력한 메시지
- `isUser: false`: 모든 NPC 메시지 (system, narr, tanjiro, user 등)

---

## 📝 주요 학습 사항

1. **LangGraph 스테이지 자동 진행 로직**
   - `max_turns`는 안전 장치로 유지하되 자동 진행은 신중히 사용
   - 대화형 시스템에서는 유저 주도 진행이 중요

2. **LLM Beats 생성 조건**
   - `llm_beats=true`는 강력하지만 유저 입력 체크 필수
   - 자동 생성은 immersive experience를 해칠 수 있음

3. **characterId 네이밍 컨벤션**
   - 'user'는 흔한 이름이지만 혼란을 야기할 수 있음
   - 'player_character' 또는 'tsuguko' 같은 명확한 이름 고려 필요

---

## 🚀 다음 단계

### 권장 개선사항

1. **Scenario 파일 리팩토링**
   - `characterId: 'user'` → `characterId: 'tsuguko'`로 변경 고려
   - 더 명확한 네이밍으로 혼란 방지

2. **스테이지 진행 UI 추가**
   - max_turns 도달 시 "다음 스테이지로 진행하시겠습니까?" 버튼 표시
   - 명시적인 유저 선택 제공

3. **LLM Beats 프롬프트 개선**
   - 유저 입력을 더 잘 반영하는 프롬프트 설계
   - context 품질 향상

### 추가 테스트 필요

- [ ] 모든 시나리오 스테이지에서 자동 진행 문제 없는지 확인
- [ ] 다양한 유저 입력 패턴 테스트
- [ ] 엣지 케이스: 빈 입력, 특수문자, 긴 입력 등

---

## 📌 참고사항

**시나리오 구조:**
- TRAIN_PRELUDE (open_narrative) → HEROES_ARRIVE (scene with llm_beats) → RENGOKU_TRAIN_DIALOGUE → ...

**주요 로그 패턴:**
```
[INFO] [OPEN_NARRATIVE] ⚠️  Turn limit reached: 4/3 (auto-advance disabled)
[INFO] [CHILDREN] 🎭 LLM Beats mode enabled for stage=HEROES_ARRIVE
[INFO] [CHILDREN] ✨ Generated 8 beats via LLM based on user input
```

**백엔드 서버:** http://localhost:8000
**프론트엔드:** http://localhost:5173

---

## ✅ 체크리스트

- [x] 문제 원인 분석 완료
- [x] open_narrative_stage.py 수정
- [x] children_agent.py 수정
- [x] ChatInterface.tsx 수정
- [x] 백엔드 서버 재시작 및 테스트
- [x] 프론트엔드 hot reload 확인
- [x] 문서화 완료 (tm_edit_2510281700.md)

---

**작성일:** 2025-10-28
**마지막 업데이트:** 2025-10-28 17:00
**작업 브랜치:** remake
