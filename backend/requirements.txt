# ========================================
# AI 인플루언서 에이전트 시스템 Dependencies
# ========================================

# === 웹 프레임워크 ===
fastapi==0.109.0
uvicorn[standard]==0.27.0
websockets==12.0
python-multipart==0.0.6
pydantic>=2.10.0
pydantic-settings>=2.0.0
slowapi==0.1.9  # Rate limiting for FastAPI

# === LLM 및 AI ===
anthropic>=0.18.0
openai>=1.0.0
langchain>=0.1.6
langchain-core>=0.1.22
langchain-anthropic>=0.1.1
langgraph>=0.0.20
langchain-openai>=0.0.5
numpy>=1.24.0  # 벡터 연산 (임베딩용)

# === 데이터베이스 ===
sqlalchemy==2.0.25
asyncpg==0.29.0  # PostgreSQL async driver
aiosqlite==0.19.0
psycopg2-binary==2.9.9
pgvector==0.2.4  # PostgreSQL vector extension for embeddings
redis==5.0.1
alembic==1.13.1  # Database migrations

# === 유틸리티 ===
python-dotenv==1.0.0
pyyaml==6.0.1
python-jose[cryptography]==3.5.0  # JWT token handling
email-validator==2.1.0  # Pydantic EmailStr support

# === 날짜/시간 ===
python-dateutil==2.8.2

# === HTTP 클라이언트 ===
httpx==0.26.0
requests==2.31.0

# === 테스트 ===
pytest==7.4.4
pytest-asyncio==0.23.3

# === 암호화 ===
bcrypt==4.1.2

# === 문자열 처리 & 매칭 ===
fuzzywuzzy==0.18.0  # 퍼지 문자열 매칭 (오타 허용)
python-Levenshtein==0.25.0  # 레벤슈타인 거리 (fuzzywuzzy 성능 향상)
# py-hanspell==1.1  # 한국어 맞춤법 검사 (최신 pip과 호환 문제로 주석 처리)

# === 이미지 처리 ===
Pillow==10.2.0  # 이미지 처리 (썸네일, 메타데이터)
